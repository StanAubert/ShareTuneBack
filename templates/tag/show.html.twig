{% extends 'base.html.twig' %}

{% block title %}Tag
{% endblock %}

{% block body %}
	<h1>Tag<a class="ml-2" href="{{ path('tag_edit', {'id': tag.id}) }}">
			<i class="fas fa-edit"></i>
		</a>

	</h1>

	<table class="table">
		<tbody>
			<tr>
				<th>Id</th>
				<td>{{ tag.id }}</td>
			</tr>
			<tr>
				<th>Title</th>
				<td>{{ tag.title }}</td>
			</tr>
		</tbody>
	</table>

	<div class="row">
		<div class="col-6">
			{{ include('tag/_delete_form.html.twig') }}

		</div>
		<div class="col-6">
			<a class="btn btn-secondary float-right" href="{{ path('tag_index') }}">Retour</a>
		</div>
	</div>


{% endblock %}
